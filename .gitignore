node_modules
.next
dist
build
!desktop/build
.turbo
.venv*
__pycache__
*.pyc
.env
.env.local
.env.*.local
coverage
tmp
apps/api/.local
# Stray double-nested state dir from running uvicorn/pytest with cwd=apps/api instead of the
# repo root (config.py's paths assume repo-root cwd) - see packaging-and-installation.md.
apps/api/apps
.pytest_cache
e2e/test-results
e2e/playwright-report
*.log
desktop/release
desktop/resources
*.tsbuildinfo

# Internal/maintainer-only docs - architecture, feature specs, superseded historical design docs,
# the agent handoff backlog, and dev-tooling scripts docs. Not relevant to someone who just wants
# to install and use Atlas; README.md links only to docs/user-guides/ for that audience. Kept
# locally (not deleted) for whoever is actively developing the app - just not published.
docs/build-and-run/
docs/feature-specs/
docs/historical/
docs/marketing/
docs/production-todo.md
